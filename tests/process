ncs && !s;

AG(ncs || try || cs);

AG(ncs -> (!try && !cs));

AG(try -> (!ncs && !cs));

AG(cs -> (!try && !ncs));


AG(ncs -> AX try);


AG((try && s) -> AX (cs && s));

AG((try && !s) -> AX try);


AG(cs -> AX(ncs && !s))

